import { createSlice } from '@reduxjs/toolkit';

let cart = createSlice({
    name: 'cart',
    initialState: [
        
    ],
    reducers: {
        
    }
})

export let {  } = cart.actions

export default cart;